
<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-xs-offset-2 col-xs-8 col-md-offset-3 col-md-6 mt-5">
      <div class="card">
       <div class="card-header">ログイン</div>
       <div class="card-body">
          <%= form_with model: @user, url: user_session_path, id: 'new_user', class: 'new_user justify-content-center', local: true do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class="form-group">
              <%= f.label :メールアドレス %><br />
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
            </div>

            <div class="form-group">
              <%= f.label :パスワード %>
              <% if @minimum_password_length %>
              <em>(<%= @minimum_password_length %> 文字以上)</em>
              <% end %><br />
              <%= f.password_field :password, autocomplete: "new-password", class: "form-control" %>
            </div>

            <div class="form-group">
              <%= f.label :パスワード（確認） %><br />
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
            </div>

            <div>
              <%= f.submit "ログイン", class: "btn btn-success btn-block" %>
            </div>
          <% end %>

          <p>アカウントをお持ちでない方はこちらから<%= link_to "新規登録", new_user_registration_path, style: "color :#4fd2c2;"%>してください</p>
        </div>
      </div>
    </div>
  </div>
</div>
